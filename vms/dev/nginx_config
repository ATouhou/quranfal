server {
    listen 8000;
    server_name 0.0.0.0;

    location = /favicon.ico {
        access_log off;
        log_not_found off;

    }

    location /static/ {
        alias /home/edgleweb/static_collected/;
        #root /home/edgleweb;
    }

    location / {
#       proxy_pass http://127.0.0.1:8001; #for runserver/gunicorn
#       add_header X-Static miss;

        include uwsgi_params;		# for uWSGI
        # uwsgi_pass 127.0.0.1:8001; #slower
        uwsgi_pass unix:/home/vagrant/edgleweb.sock; # this does not work, work on later
    }

}